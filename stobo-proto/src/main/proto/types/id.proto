syntax = "proto3";

package stobo.types;

option java_multiple_files = true;
option java_outer_classname = "IdProto";
option java_package = "com.stobo.proto.types";

// Wrapper around an identity. This is for internal usage only. Clients
// should use strings and that will later be decoded to this type on the server.
message Id { optional uint64 seq_id = 1; }
